---
title: clousx6 钓鱼
date: 2023-05-2 5:51:00
categories: robot
top_img:
keywords : php 钓鱼游戏
cover: https://image.muerg.cn/2/2023/05/14/646057f70a1fb.jpg
---



#### menu.php

//菜单

```php
<?php
$msg = $_REQUEST["msg"];
if (!empty($msg)) {
    switch ($msg) {
        case "基本功能";
            echo "钓鱼";
            break;
        case "钓鱼商店";
            echo "购买鱼竿";
            echo "<pre>";
            echo "购买鱼饵";
            echo "<pre>";
            echo "购买鱼线";
            echo "<pre>";
            break;
        case "无";
            echo "无";
            break;

        default:
            echo "暂无匹配相同菜单";
    }
} else {
    echo "参数不能为空";
}
```



#### goFishing.php

//钓鱼

```php
<?php
$money = $_REQUEST["money"];//💰数
$bait = $_REQUEST["bait"];//鱼饵
$rod = $_REQUEST["rod"];//鱼竿
$type = $_REQUEST["type"];//返回类型
$id = $_REQUEST["qq"];//用户qq
$name = $_REQUEST["name"];//用户名字

$deep = rand(1, 5);
$undeep = rand(1, 5);
if ($bait < 2 or $rod < 0) {
    if ($type = "test") {
        echo "用户" . $name;
        echo "qq" . $id;
        echo "鱼饵和鱼竿不足";
    } else {
        echo json_encode(["name" => $name, "qq" => $id, "msg" => "鱼饵或鱼竿不足"], JSON_UNESCAPED_UNICODE);
    }
} elseif ($money > 10) {
    switch ($deep) {
        case 1;
            if ($type == "test") {
                echo "用户" . $name;
                echo "qq" . $id;
                echo "恭喜在深海你钓中了小黄鱼";
            } else {
                echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你钓中了小黄鱼"], JSON_UNESCAPED_UNICODE);
            }
            break;
        case 2;
            if ($type == "test") {
                echo "用户" . $name;
                echo "qq" . $id;
                echo "恭喜你在深海钓中了恭喜你钓中了大黄鱼";
            } else {
                echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你钓中了大黄鱼"], JSON_UNESCAPED_UNICODE);
            }
            break;
        case 3;
            if ($type == "test") {
                echo "用户" . $name;
                echo "qq" . $id;
                echo "恭喜你在深海钓中了罗非鱼";
            } else {
                echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你钓中了罗非鱼"], JSON_UNESCAPED_UNICODE);
            }
            break;
        case 4;
            if ($type == "test") {
                echo "用户" . $name;
                echo "qq" . $id;
                echo "恭喜你在深海钓中了鲨鱼";
            } else {
                echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你钓中了鲨鱼"], JSON_UNESCAPED_UNICODE);
            }
            break;
        default:
            if ($type = "test") {
                echo "匹配不上";
            } else {
                echo json_encode(["msg" => "匹配不上"], JSON_UNESCAPED_UNICODE);
            }
    }
} else {
    switch ($undeep) {
        case 1;
            if ($type == "test") {
                echo "用户" . $name;
                echo "qq" . $id;
                echo "恭喜你在深海钓中了草鱼";
            } else {
                echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你钓中了草鱼"], JSON_UNESCAPED_UNICODE);
            }
            break;
        case 2;
            if ($type == "test") {
                echo "用户" . $name;
                echo "qq" . $id;
                echo "恭喜你钓中了鲢鱼";
            } else {
                echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你钓中了鲢鱼"], JSON_UNESCAPED_UNICODE);
            }
            break;
        case 3;
            if ($type == "test") {
                echo "用户" . $name;
                echo "qq" . $id;
                echo "恭喜你钓中了鳙鱼";
            } else {
                echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你钓中了鳙鱼"], JSON_UNESCAPED_UNICODE);
            }
            break;
        case 4;
            if ($type == "test") {
                echo "用户" . $name;
                echo "qq" . $id;
                echo "恭喜你在深海钓中了月亮";
            } else {
                echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你钓中了月亮"], JSON_UNESCAPED_UNICODE);
            }
            break;
        default:
            if ($type = "test") {
                echo "匹配不上";
            } else {
                echo json_encode(["msg" => "匹配不上"], JSON_UNESCAPED_UNICODE);
            }
    }
}





```


#### shopBait.php

//买鱼饵

```php
<?php
$price = $_REQUEST["price"];//鱼饵的价格
$num = $_REQUEST["num"];//数量
$money = $_REQUEST["money"];//需要读取用户的钱

$allPrice = $num * $price;
if ($allPrice > $money) {
    echo "你的钱数无法购买";
} else {
    echo "购买成功";
    echo "<pre>";
    echo "鱼饵数量" . $num;
    echo "<pre>";
    echo "鱼饵单价" . $money;
    echo "<pre>";
    echo "总共花费" . $allPrice;
}


```


#### Shoplibne.php

//买鱼线

clousx6.cn/myapi/fish/shopBait.php

与买鱼饵一样

priceLine 鱼线价格

numLine鱼线数量

money用户钱

```php
<?php
//购买鱼线

$price = $_REQUEST["priceLine"];//鱼线的价格
$num = $_REQUEST["numline"];//数量
$money = $_REQUEST["money"];//需要读取用户的钱数
$allPrice = $num * $price;
if ($allPrice > $money) {
    echo "你的钱数无法购买";
} else {
    echo "购买成功";
    echo "<pre>";
    echo "鱼饵数量" . $num;
    echo "<pre>";
    echo "鱼线单价" . $money;
    echo "<pre>";
    echo "总共花费" . $allPrice;

}


```

#### shopRod

//鱼竿

```php
<?php

//购买鱼竿

$price = $_REQUEST["priceRod"];//鱼竿的价格
$num = $_REQUEST["numRod"];//数量
$money = $_REQUEST["money"];//需要读取用户的钱数

$allPrice = $num * $price;
if ($allPrice > $money) {
    echo "你的钱数无法购买";
} else {
    echo "购买成功";
    echo "<pre>";
    echo "鱼竿数量" . $num;
    echo "<pre>";
    echo "鱼鱼竿单价" . $money;
    echo "<pre>";
    echo "总共花费" . $allPrice;
}


```

#### Shopfish.php

//卖鱼

```php
<?php
$id = $_REQUEST["qq"];//用户qq
$name = $_REQUEST["name"];//用户名字
$fishType = $_REQUEST["fish"];//卖的鱼
$numberFish = $_REQUEST["num"];//鱼的个数
$factFish = $_REQUEST["usenum"]; //用户真实的鱼数
$money = rand(1, 1000);

if ($numberFish == 1 and $factFish > $numberFish) {
    switch ($fishType) {
        case "小黄鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售了小黄鱼", "number" => "1", "money" => $money], JSON_UNESCAPED_UNICODE);
            break;
        case "大黄鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售了大黄鱼", "number" => "1", "money" => $money], JSON_UNESCAPED_UNICODE);
            break;
        case "罗非鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售了罗非鱼", "number" => "1", "money" => $money], JSON_UNESCAPED_UNICODE);
            break;
        case "鲨鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售了鲨鱼    ", "number" => "1", "money" => $money], JSON_UNESCAPED_UNICODE);
            break;
        case "草鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售了草鱼    ", "number" => "1", "money" => $money], JSON_UNESCAPED_UNICODE);
            break;
        case "鲢鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售了鲢鱼   ", "number" => "1", "money" => $money], JSON_UNESCAPED_UNICODE);
            break;
        case "鳙鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售了鳙鱼    ", "number" => "1", "money" => $money], JSON_UNESCAPED_UNICODE);
            break;
        default;
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "匹配不上   ", "number" => "1", "money" => $money], JSON_UNESCAPED_UNICODE);
    }

} elseif ($numberFish > 1 and $factFish > $numberFish) {
    $fishMoney = $money * $numberFish;
    switch ($fishType) {
        case "小黄鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售小黄鱼", "number" => $numberFish, "money" => $fishMoney], JSON_UNESCAPED_UNICODE);
            break;
        case "大黄鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售大黄鱼", "number" => $numberFish, "money" => $fishMoney], JSON_UNESCAPED_UNICODE);
            break;
        case "罗非鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售罗非鱼", "number" => $numberFish, "money" => $fishMoney], JSON_UNESCAPED_UNICODE);
            break;
        case "鲨鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售了鲨鱼   ", "number" => $numberFish, "money" => $fishMoney], JSON_UNESCAPED_UNICODE);
            break;
        case "草鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你出售了草鱼    ", "number" => $numberFish, "money" => $fishMoney], JSON_UNESCAPED_UNICODE);
            break;
        case "鲢鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你钓中了鲢鱼   ", "number" => $numberFish, "money" => $fishMoney], JSON_UNESCAPED_UNICODE);
            break;
        case "鳙鱼";
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "恭喜你钓中了鳙鱼    ", "number" => $numberFish, "money" => $money], JSON_UNESCAPED_UNICODE);
            break;
        default;
            echo json_encode(["name" => $name, "qq" => $id, "msg" => "匹配不上   ", "number" => $numberFish, "money" => $money], JSON_UNESCAPED_UNICODE);

    }
} else {
    echo "鱼不足";
}





```



#### 更新日志

{% timeline 2023 %}
<!-- timeline 01-01 -->

增加返回json类型,优化买鱼竿鱼线鱼饵bug，添加参数id,name,目前x6词库开发中

{% timeline 2023 %}

<!-- timeline 01-02 -->

增加卖鱼功能 x6词库开发中

{% timeline 2023 %}

<!-- timeline 06-30 -->

重构代码 结合数据库

<!-- endtimeline -->

{% endtimeline %}
